[project]
name = "scene-reader-worker"
version = "0.1.0"
description = "SceneReader Python worker skeleton"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
