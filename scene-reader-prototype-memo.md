# 阅境 SceneReader 原型决策备忘录

## 目的

这份备忘录用于明确“阅境 SceneReader”的第一版原型方向。

当前目标不是设计完整 App，而是先判断应该做哪一种原型。判断依据是早期最重要的风险：AI 是否能够可靠识别现代中文小说中的地点或环境变化，并把插图放在文本中的合适位置。

## 推荐的第一版原型

建议先做“单章 HTML 图文版故事书”原型。

这个原型以 TXT 或 EPUB 书籍为输入，只处理其中一章，完整保留原文章节内容，识别清楚的地点或环境变化，并在相关段落之后插入生成的场景插图。

选择这个方向的原因是：它足够接近产品愿景，同时验证范围仍然可控。它可以测试 SceneReader 是否能把纯文字转化为有用的视觉阅读辅助，而不需要一开始就开发完整阅读器、整本书处理、人物追踪或复杂交互。

## 核心验证问题

系统能否识别现代中文小说单章中明显的地点或环境变化，并在正确段落之后插入插图？

第一版原型主要验证“插图位置是否正确”，而不是完整阅读体验是否沉浸，也不是最终产品是否有商业吸引力。

## 输入范围

原型设计应面向 TXT 和 EPUB 输入。

第一版只需要从导入书籍中处理一章。文档不绑定固定样本文本，但默认对象是现代中文小说。第一阶段暂不覆盖古文、强实验性文本、复杂翻译文学等更难泛化的文本类型。

## 输出范围

主要输出为一个 HTML 页面，包含：

- 完整原文章节
- 插入在地点或环境变化之后的插图
- 候选场景列表
- 实际插图列表

选择 HTML 是因为它便于预览，图文排版灵活，也比原始数据文件更接近未来的阅读体验。

## 场景选择规则

系统应先识别候选地点或环境变化。

在候选场景中，系统自动筛选最多 5-8 个最清楚的场景变化用于插图。如果一章中清楚的场景变化少于 5 个，不强行补图。

如果候选场景超过 8 个，优先保留地点或环境变化最明确的场景，例如从家到公司、从室内到街道、从一个具名地点到另一个具名地点。

第一版不把同一地点内的情绪或事件变化作为插图触发点。例如“客厅里争吵”到“客厅里沉默”，如果环境没有变化，不应触发新的插图。

## 候选场景列表

每个候选场景应展示：

- 原文片段
- 识别出的地点或环境变化
- 生成的图片提示词

候选场景列表用于检查漏检、误检，以及自动筛选逻辑是否选中了合适的插图点。

## 图片生成

原型应生成少量图片样张，而不只是输出提示词。

每个入选场景都应生成接近可直接用于生图的提示词。提示词应包含画面主体、环境、光线、视觉风格、构图指引，以及必要的避免项。

第一版图片风格选项为：

- 写实
- 动漫
- 插画

开放式风格输入、水墨风、像素风暂不纳入第一版。

## 地点一致性

第一版需要关注地点一致性，但只在同一地点于单章内重复出现时启用。

如果同一地点重复出现，后续该地点的图片应尽量参考首次生成的图片，使用参考图或种子图来保持空间观感一致。如果单章内没有重复地点，这项要求不应增加额外复杂度。

第一版暂不要求角色形象一致。

## 通过标准

如果满足以下中等要求，可以认为该原型方向值得继续：

- 大多数明确的地点或环境变化能被识别出来。
- 大多数插图能出现在合适段落之后。
- 少量漏检或误插可以接受。
- 生成图片基本能从原文片段和提示词中解释。
- 如果存在重复地点，图片之间能体现一定的空间连续性。

如果系统频繁漏掉地点变化、把大量非地点变化误判为插图点，或生成场景无法从相关文本中解释，则说明这一原型表现较弱。

如果验证失败，备忘录只记录失败原因，不展开完整调整路线。

## 非目标

第一版不做完整阅读 App，不处理整本书，不推断人物关系，不支持复杂剧情或情绪触发插图，不保证角色身份一致，也不优先优化生产成本和速度。

## 附录：考虑过的备选原型

### 解析验证原型

只验证 TXT/EPUB 到候选场景和提示词，不生成最终 HTML 图文章节。

优点是成本更低，也更聚焦文本理解；缺点是离产品体验较远，无法判断生成场景放回阅读流之后是否有用。

### 阅读体验原型

做一个简易阅读界面，展示文字和生成图片。

它更接近最终 App，但容易把第一阶段扩展到 UI、阅读控制、分页和交互细节，反而在“插图点是否正确”这个核心问题还没搞清楚之前扩大范围。

### 视觉摘要原型

把一章或一本书压缩成若干图文场景卡片。

它可能更容易产出一个好看的成品，但会把方向从“辅助阅读”转向“内容摘要”。因此它不适合作为验证 SceneReader 原始想法的第一版原型。
